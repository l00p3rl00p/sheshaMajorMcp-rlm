# Release Readiness Checklist

## 1. Quality & Tests
- [x] Python tests pass (`pytest -v`) - 449 passed.
- [x] GUI tests pass (`npm run test:ci`) - 10 passed.
- [x] Mypy typechecks pass (`mypy src/shesha`).
- [x] Ruff linting and formatting pass (`ruff check .`).
- [x] `make all` passes in a clean environment.

## 2. Resource Management & Security
- [x] Docker clients use `try/finally` for cleanup (Verified in `Shesha` and `Librarian CLI`).
- [x] No hardcoded API keys or secrets in codebase.
- [x] `?raw` imports in GUI are verified for safety.
- [x] Error boundaries protect GUI from component-level crashes.

## 3. Documentation & Onboarding
- [x] `README.md` is updated with GUI testing instructions.
- [x] `ARCHITECTURE.md` describes the current state of GUI prototypes and testing.
- [x] `USER_OUTCOMES.md` defines success for the operator GUI.
- [x] `CHANGELOG.md` reflects all recent changes under `[Unreleased]`.
- [x] `mcp-server-readme.md` is accurately generated by the installer.

## 4. Release Integrity
- [ ] Version number is chosen (Suggested: `v0.5.0`).
- [ ] CHANGELOG.md entries moved from `[Unreleased]` to a versioned section.
- [ ] No force-pushes or amended tags since last review.
- [ ] System audit confirms Python 3.11+ requirement is enforced.

## 5. GUI Verification (Visual)
- [x] Navigation flows work (Verified via integration tests).
- [x] Prototype status is clearly documented in code for future devs.
- [x] "Connect" toggle and backend bridge plans are documented in `Wishlist.md`.
